import styled from "styled-components";

export const LoggedInUserDetailsContainer = styled.div`
  align-items: center;
  display: flex;
  justify-content: space-between;
  border: 2px solid currentColor;
  border-radius: var(--BORDER-RADIUS);
  padding: 1rem;
`;
